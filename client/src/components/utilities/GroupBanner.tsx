import { IRoomDetails } from "@/core/lib/types/schema";

const GroupBanner = ({ group }: { group: IRoomDetails }) => {
  console.log(group);

  return <div>GroupBanner</div>;
};

export default GroupBanner;
